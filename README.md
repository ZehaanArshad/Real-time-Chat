# Real-time Chat

A MERN-stack real-time chat app: React (Vite) client, Node/Express + Socket.io server, MongoDB/Mongoose for persistence, JWT + bcrypt for auth.

Setup and usage docs land at the end of the build (see `plan.md`).
